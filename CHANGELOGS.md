# Ver1.1.0 Changelogs

- Patched some subtle bugs revolving around `save()` and `fetch()`
- Introduced the new `load()` function
- Added indenting to `save()` and `tojson()`
- Now non-absolute addresses for `load()`, `save()` and `fetch()` now defaults from the script location
- Introduced `--patch-version` for terminal commands